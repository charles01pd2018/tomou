const CenterPanel = ({
    id,
    content
}) => {

    return (
        <section id={id} className='center-panel-container'>
            <div className='center-panel-wrapper'>
                
            </div>
        </section>
    );
}