// pages
import Home from './home';

// layout
import DefaultLayout from '../layout/defaultLayout';

export default ({
}) => {
  return (
    <DefaultLayout>
      <Home />  
    </DefaultLayout>
  );
}