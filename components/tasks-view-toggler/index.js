const TasksViewToggler = ({
    id,
    content
}) => {

    return (
        <section id={id} className='tasks-view-toggler'>
            Tasks View Toggler
        </section>
    );
}

export default TasksViewToggler;