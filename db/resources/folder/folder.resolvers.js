
const foldersList = ( _, args, ctx ) => {

}

const newFolder = ( _, args, ctx ) => {

}

const deleteFolder = ( _, args, ctx ) => {

}

export default {
    Query: {
        foldersList,
    },
    Mutation: {
        newFolder,
        deleteFolder,
    }
}